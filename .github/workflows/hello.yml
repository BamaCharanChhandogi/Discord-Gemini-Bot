on:
  issues:
    types: [opened, edited]
  pull_request:
    types: [opened, edited]

jobs:
  keyword-listener:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Keyword Listener
        uses: BamaCharanChhandogi/hello_world@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          keyword: 'Hello I am bama '